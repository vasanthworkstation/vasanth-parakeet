# Plan 030 — AI Polish Overhaul: Two Personas, Wispr-Grade Defaults

Status: **Phases 1–3 SHIPPED + pushed (2026-07-04).** Branch `feat/ai-polish-personalization` off `main`, 21 commits, pushed to origin, gates green (cargo 1188 / clippy clean / TS+lint clean). Phase 3 = one Polish screen (Polish / Static Rules / Advanced) + guided BYOK + simplified onboarding + tray/pill + retired mode-shortcuts; both stop-review toggle bugs fixed. **Phase 4 RESEARCHED + blueprinted (§12), 2 founder forks pending (CLI set, spawn model) — no Codex on 4C until resolved.** Original approval: v2 (2026-07-02) — sequencing ✓, name = "Polish" ✓, guided BYOK in the Polish screen (NOT onboarding) ✓. Basis: 4-agent research pass (2026-07-02) — pipeline architecture audit, UX surface audit, competitive benchmark (Wispr Flow/superwhisper/VoiceInk/Aqua/FluidVoice/OS-native), prompt-quality critique. Code is the source of truth; older plans/ docs are non-binding.

## 1. Problem (re-diagnosed)
Three failures, in order of user harm:
1. **Discovery/setup wall.** Onboarding never mentions AI polish (`OnboardingDesktop.tsx:283-306` — no AI step). Enabling it is a self-discovered 6–7 step BYOK gauntlet (find screen → provider → external signup → key → model from up to 49 → toggle). Lock icon on AI modes (`EnhancementSettings.tsx:766`) reads as paywall next to the Upgrade-to-Pro onboarding screen. Most users never see polished output.
2. **Decision-layer spaghetti.** "Which enhancement runs" is encoded 3× (`EnhancementPreset` prompts.rs:109, mirror `WritingMode` writing.rs:31, TS union ai.ts:3), resolved in 4 functions, `ai_enabled`×preset clamped in 6 sites (prompts.rs:183, writing.rs:414/427, shortcuts.rs:287, lib.rs:1698, EnhancementsSection.tsx:129/454), Rust↔TS migration logic hand-mirrored, cache invalidation manual across 8 call sites. UI: 16 concepts, 2 sidebar screens, 4 names for one feature, Voice Commands referenced but uneditable, 8 formatting shortcuts shipped unbound.
3. **Quality levers unset.** No temperature (provider default ~1.0) / no max_tokens anywhere in `src-tauri/src/ai/`; `"stream": false`; blocking insert behind full LLM response (30s budget, ai.rs:1042); `sanitize_ai_output` (executor.rs:150) strips only control chars — model preambles get typed at the cursor; captured active-app context stops at preset selection and never reaches the prompt; instrumentation = one log line.

Competitive read: Wispr Flow wins on **restraint-first cleanup + ambient app-context injection + proper-noun auto-learning + <700ms p99**; its liabilities are privacy (screenshot scandal), reliability (75+ outages), $15/mo. Open position nobody holds: **Wispr-grade defaults + local-first trust + BYOK economics + cross-platform.** superwhisper/VoiceInk lose on defaults, not capability — "powerful but must configure" is punished; that is VoiceTypr today.

## 2. Goals
1. Two personas, one rule: **regular user = one switch + automatic behavior; advanced user = an Advanced layer that overrides it.** Architecture must make new features cheap on both sides.
2. Polish quality at Wispr grade on the same BYOK models: restraint contract ("edit like a copyeditor, never a ghostwriter"), hardened output contract, app-aware formatting.
3. One name, one screen, guided setup. Zero silent failure: user can always tell whether text was polished.
4. Latency measured, then attacked. Smooth dictation UX is the core service; polish must never make it feel worse.

## 3. Non-goals (this plan)
Learning loop / fix-last (Phase 5 roadmap — surface built here, feature parked). No local polish model, no bundled cloud (pending §9.3). No fine-tuning. No screen OCR. No new modes. No transcript content in telemetry.

## 4. Target architecture
- **Single source of truth:** `EnhancementPreset` is the one encoding; `PersonalDictation` = polish off; `ai_enabled` derived at read time; `WritingMode` deleted.
- **One resolver:** pure `resolve_pipeline_config(settings, active_app, key_state) -> EffectiveConfig`, called by recording, shortcuts, UI. Kills the 6-site clamp.
- **Module split:** `writing.rs` (~3.9k lines) → `writing/settings.rs`, `writing/library_rules.rs` (deterministic engine), `stt/vocabulary.rs` (4 engine compilers), `writing/pipeline.rs` (process_transcription). `ai/` stays as-is — it is the clean seam.
- **Rust↔TS contract:** golden-fixture parity test for preset migration / requires-ai / defaults / labels (prompts.rs:125-170 ↔ ai.ts:19-69).
- **Deterministic layer always on** regardless of polish state (it also feeds STT vocab — writing.rs:1302-1607); lives under Advanced.

## 5. Phases (each ships value alone; Codex implements, Claude reviews per phase)
- **Phase 1 — Quality quick wins (no UI).** temperature ≈0.2 + max_tokens in ChatOptions/custom payload; prompt hardening (no preamble/quotes/fences; return input unchanged if already clean; never answer questions; code-switching preservation; explicit translation instruction replacing the implicit `{language}` contract + `output==input` failure heuristic writing.rs:1893); executor strip/validate layer (wrapping quotes/fences/known preambles; reject-and-retry-once on refusal/length-ratio anomalies); per-stage latency telemetry (STT / deterministic / connect / inference / insert). Also: route validation probes through `AiExecutor`, retire `run_openai_chat_probe` (ai.rs:197) and confirm/delete legacy `enhance_transcription_internal` path (ai.rs:1069).
- **Phase 2 — State collapse.** §4 in full. Migration extends existing `migrate_preset_str` mechanism (already handles Coding/Email/Default); tested no-data-loss before any removal. Centralize settings mutations → one invalidation point.
- **Phase 3 — UX collapse.** One screen, one name: **Polish**. Onboarding is touched only to SIMPLIFY (founder call): remove the first-dictation test step (in-app hotkey conflicts make it fail inside our own window while working everywhere else — replace with a "you're all set, enjoy voice typing" success screen), and clean up the success/upgrade steps (name+email collection, upgrade pitch). No polish setup in onboarding — polish is optional. The guided setup lives in the Polish screen as its empty state: one card — "Polish cleans up your dictation. Connect an AI to turn it on: [Anthropic] [OpenAI] [Google] [OpenRouter]" → tap one → "Get a key (~2 min)" deep link + paste field → validate → Polish flips on with the provider's recommended fast model auto-selected. **No quality tiers / model choice on the simple surface** (founder call): Polish is on/off only; the model is auto-picked from the catalog `recommended` pair; a small educational line/link ("Which AI should I pick? →") teaches instead of exposing a knob. Full model list, thinking on/off, custom/OpenAI-compatible endpoints live in Advanced. Supported providers: Anthropic, OpenAI, Gemini, **OpenRouter (added, with its own curated recommended pair)**; reasoning behavior automatic via the existing per-model reasoning flag + `ReasoningEffort::Minimal` gating, with an Advanced thinking toggle. **Output language / translation is a regular-user control** — a card directly under the Polish switch (founder call, confirmed against the mock). **Three-tier layout** (founder call 2026-07-03, corrects the earlier "everything under Advanced"): (1) **Polish** — switch + "Using {provider}·{model}·Change" status + Final language; (2) **Static Rules** — its OWN top-level block (NOT Advanced): the deterministic layer (Words & Names, Corrections, Text Shortcuts) that works with or without AI and feeds STT vocab — essentials don't hide under Advanced; subtitle "Work with or without Polish — even better with it"; (3) **Advanced** — collapsible drawer for genuine power-user depth: Reshape-by-app (the old 6 modes survive here as per-app `AppFormattingRule`s), full model list + thinking toggle + custom/OpenAI-compatible endpoint. The "Default Formatting"/text-rules nav screen is removed; its content becomes the Static Rules block on the unified Polish screen. Retire 6 set-mode + cycle shortcuts with one-time notice (keep toggle-polish); kill lock-icon paywall look on the simple surface; pill gets a "Polishing…" label; tray shows polish state; delete orphaned Voice Commands copy (data-only, no editor). Users with stored non-Clean global preset: convert to Polish-on-Clean with one-time "reshaping moved to Advanced per-app" notice. Design approved via before/after mock 2026-07-03. **Sub-slots:** 3a structure+naming (merge to one Polish screen, remove second nav entry, rename all AI-Formatting/enhancement→"Polish" + rules→"Static Rules", three-tier reorg using existing controls, migration) → 3b guided BYOK empty state + kill paywall + reshape-by-app relocation → 3c onboarding simplification + peripheral (pill/tray/retire shortcuts).
- **Phase 4 — Context awareness.** App identity → prompt (plumbing exists to preset layer, writing.rs:1949); maintained app→category taxonomy (chat/email/docs/code) with baked-in details (strip trailing period in chat apps, structured prose in docs, camelCase in editors); local-only + transparent (history shows what was sent). Per-app overrides remain the Advanced layer on top.
- **Phase 5 — Roadmap (parked).** Streaming + latency-tiered routing (short utterance → nano tier; sibling-model fallback on bad output); proper-noun-only auto-learning dictionary (✨-flagged, deletable — the moat nobody in the open/BYOK camp has; lands on the surface built in P1–4); local polish model / bundled cloud per §9.3.

## 6. Verification
Rust: resolver unit tests (app-rule × preset × key-state matrix), migration no-data-loss/no-broken-keybinds, output-validator cases (preamble/fence/refusal/identity), translation-instruction path. Frontend: guided setup happy path, Advanced hidden by default, pill/toast on polish failure, parity golden fixtures. E2E smoke per SMOKE.md. Gates per phase: `pnpm typecheck && pnpm lint && pnpm test && cargo test && cargo clippy -- -D warnings`.

## 7. Execution model
Claude rewrites/owns this plan + reviews; Codex implements per phase in this worktree (companion task, --cwd --write). Phase boundary = gates green + Claude review + founder ping. No push without explicit instruction.

## 8. Key files
`src-tauri/src/ai/{prompts,executor,genai_runtime,openai_compatible,catalog}.rs` · `src-tauri/src/writing.rs` (split) · `src-tauri/src/commands/{ai,audio,shortcuts}.rs` · `src-tauri/src/lib.rs:1698` · `src/types/ai.ts` · `src/components/sections/{EnhancementsSection,ShortcutsSection}.tsx` · `src/components/EnhancementSettings.tsx` · `src/components/onboarding/OnboardingDesktop.tsx` · `src/components/navigation.ts` · `src/components/pill/usePillController.ts` · `src-tauri/src/menu/tray.rs`

## 9. Founder decisions — RESOLVED 2026-07-02
1. **Sequencing:** Phase 1 (quality) first, then 2→3→4. ✅
2. **Hero name:** "Polish" everywhere. ✅ (founder approves final copy per marketing rule)
3. **Setup story:** guided BYOK, but **in the Polish screen's empty state, not onboarding** — onboarding stays STT-only and simple; polish is optional. Cloud-only for polish in v1; local polish model stays roadmap. ✅
4. **Model complexity (revised 2026-07-02):** NO tier chips on the simple surface — Polish on/off only, model auto-picked from the provider's catalog `recommended` fast entry; educational "which AI?" link instead of a knob. Full model list + thinking toggle + custom endpoints in Advanced. Providers: Anthropic, OpenAI, Gemini + OpenRouter. ✅
5. **Output language/translation:** regular-user surface, not Advanced. ✅
6. **Onboarding cleanup (same release):** remove first-dictation test step (in-app hotkey conflict bug) → "you're all set" success screen; trim name/email + upgrade steps. ✅

## 10. Agent CLI as a polish provider — RESEARCHED 2026-07-02, verdict: SHIP AS OPT-IN BETA (own phase, after Phase 3)
Detect installed agent CLIs and offer them as a **no-API-key** polish provider via a third `AiRuntime` (`ai/cli_runtime.rs`). Two live-tested research passes on founder's machine. **Ship order (research-final 2026-07-02): Pi → Claude Code → Codex.**
- **Pi (earendil/pi, ex badlogic) — GO, ship FIRST.** `pi -p --no-tools --no-session -ne -ns -nc --thinking off --system-prompt … --mode json`; measured **109 input tokens/call, 3.37s e2e (0.41s CLI startup — model API dominates)**; full system-prompt replace by design; OAuth reuse for **Claude Pro/Max + ChatGPT + Copilot** plus 20+ BYOK providers; `--mode rpc` for a warm stdio process. Watch: npm rename in flight, single dominant author (company-backed since Apr 2026); pin versions.
- **Claude Code — GO, second (founder's quota theory CONFIRMED).** Trimmed call (`--system-prompt` + `--tools "" --strict-mcp-config --no-session-persistence --safe-mode`, empty cwd) measured **169 input tokens ($0.0015, 3.5s)** vs **~26.7k tokens ($0.0219, 5.8s)** with the default harness — a 158× reduction; polish becomes a rounding error on a Pro/Max plan. ToS still gray (exec-binary tolerated, token extraction banned — NEVER touch keychain); keep the kill-switch + disclaimer. `--bare` (fast path) remains API-key-only, no date for subscription support.
- **Codex CLI — GO, third, with metered-plan warning.** 3.3–4.9s; OpenAI explicitly permits plan reuse; `model_instructions_file` truly replaces base instructions BUT tool schemas + user's skills catalog + env context are non-removable → **~9.4k tokens/call floor** (best trim from 12.5k); warn users it drains ChatGPT plan quota at dictation frequency. `codex app-server` daemon (first-party) for warm mode.
- **Amp — dropped (founder call 2026-07-02;** docs-confirmed NO-GO anyway: `-x` consumes paid credits only, no system-prompt control, ~$0.025–0.03/call). **Replaced by oh-my-pi (OMP) — GO (researched 2026-07-02): same adapter as pi.** Fork of pi by can1357 (15.6k★, binary `omp`, ~3 releases/day): keeps `-p --mode json --system-prompt --no-tools -m provider/model` 1:1 and `--mode rpc` warm framing. Adapter deltas only: append `--no-session --no-skills --no-rules --no-extensions --no-lsp --no-title`, empty temp cwd, and treat auth as separate from pi (omp stores OAuth in `~/.omp/agent/agent.db` — user must `/login` in omp once; setup UX must say so). Broader subscription reuse than pi (adds Cursor, Copilot, GLM/Kimi coding plans). Prefer `-p` one-shot; `--mode rpc` later (open RPC-lifecycle issues). Feature-detect via `omp --version`; validate JSON event schema with one live call at setup.
- **OpenCode — NO-GO for spawn-per-call; revisit via `opencode serve` only if demanded.** Root-caused: `run` silently retries failing APIs forever without printing the error (open bug class #8203/#17516) + env-var footgun; `serve` + `POST /session/:id/message` (per-request model/system/tools) is the only sane integration.
- **Gemini CLI — dropped** (founder never asked; headless auth hangs anyway). Cursor CLI works but 11.3s; Copilot CLI (`copilot -p`, officially programmatic) optional later.
- **Latency (founder-confirmed approach): warm-by-default.** Cold spawn is the bottleneck, not the model (claude -p with haiku: 4.5–5s wall, only ~0.65s CPU — rest is CLI init + auth + harness). So when the CLI provider is selected, keep a **persistent warm session** (claude `-p --input-format stream-json` long-lived process / `codex app-server` daemon — the same mechanism their own IDE integrations use), started at app launch / recording start alongside the existing connection warming, recycled every ~20 requests to stop context accumulation. Realistic warm per-call: **1.5–3s** ("a couple of seconds"). Direct API (300–900ms) remains the recommended default; CLI provider labeled "No API key needed — uses a coding CLI you already have. Slightly slower, shares your subscription limits."
- **Hidden cost:** ~12.5k input tokens per polish call (CLI harness prompt) — drains 5-hour subscription windows; add a per-session polish budget + UI note.
- **Mandatory hardening:** hard 8–10s kill-timeout → raw-transcript fallback; empty temp cwd + isolation flags; pinned JSON output validated; version/feature detection at setup (flag drift is real — `--max-turns` already removed); auth preflight cached at setup time, never per-dictation.

## 11. Phase 2 — VALIDATED inventory (research 2026-07-02, post-Phase-1) + sub-slot split
**De-risking facts (ground truth, not assumption):**
- **Serde trap is mostly a non-issue.** `EnhancementPreset` serializes **PascalCase** (no `rename_all`, custom `Deserialize`→`migrate_preset_str`); `WritingMode` serializes **snake_case** (`rename_all="snake_case"` + `alias="coding"`). The ONLY place `WritingMode`'s snake_case hits disk is **history metadata `mode`** (`audio.rs:1108-1111`) — and grep found **no reader** deserializes it back (display-only; frontend never reads `.mode`). So retyping `WritingResult.mode`/`WritingProfile.mode` to `EnhancementPreset` is cosmetic/forward-only. Must update 2 characterization tests that hard-assert snake_case: `audio.rs:2293, 2338`.
- **`AppFormattingRule.preset`** (the one preset value persisted in user settings) is **already `EnhancementPreset` PascalCase** — no migration needed.
- **No stored data holds snake_case presets** → deleting `WritingMode` needs NO new snake_case deserialize path. Legacy values to keep accepting stay PascalCase: `Coding/Prompts/Commit→Code`, `Email→Writing`, `Default`→ai-dependent, unknown→PersonalDictation (`prompts.rs:128-147`).
- **Remote protocol carries no preset/mode** (`remote/*` = STT model/engine/lang/task only; preset resolved locally on desktop) → deleting `WritingMode` cannot break a peer.
- **`ai_enabled` store key MUST keep being written** (frontend reads via `get_ai_settings`→`AISettings.enabled` at `ai.rs:250,302`; listens `ai-enabled-changed` from `shortcuts.rs:297,306`). "Derived" applies to the recording/resolution path only.
- **6-site clamp** (`!ai_enabled && preset.requires_ai → PersonalDictation`) lives at `prompts.rs:186-188`, `writing.rs:426-428`, `writing.rs:439-441`, `EnhancementsSection.tsx:129` → `resolve_pipeline_config` absorbs the backend three.
- **Cache invalidation** = 8 manual sites (`ai.rs:724,759,806,824`; `shortcuts.rs:295,304` + null-write `:252`; `settings.rs:743`) → one centralization point. Note `lib.rs:1720/1732` startup force-disable does NOT invalidate (cache built later — fine).

**Split into two sub-slots (review safety):**
- **Phase 2a — mechanical module split of `writing.rs` (~3925 lines), ZERO behavior change.** `writing.rs` → `writing/mod.rs` (module decls + `pub use` re-exports so every `crate::writing::*` path stays valid — NO file outside `writing/` changes) + `writing/settings.rs` (structs/serde/sanitize/load-save, ~22-403) + `writing/library_rules.rs` (deterministic engine, ~547-1230+1633-1818) + `writing/vocabulary.rs` (the 4 STT compilers, ~1247-1632 — keep under `writing/` not `stt/` for path stability) + `writing/pipeline.rs` (resolution + smart formatting + `process_transcription`, ~399-546+1231+1820-2104). Move each `#[cfg(test)]` group ALONGSIDE its code (preserves private access; widen to `pub(crate)` only where forced). **Gate proof: test count stays exactly 1190; clippy -D warnings clean.**
- **Phase 2b — semantic state collapse (after 2a committed).** Delete `WritingMode` + both `From` impls + its dup `requires_ai_formatting`; retype the 2 struct fields to `EnhancementPreset`; fix the 3 production matches + `audio.rs` test builders + the 2 snake_case assertions. Add `resolve_pipeline_config(settings, active_app, key_state)` absorbing the 3 backend clamp sites + the 4 resolver fns (`resolve_app_formatting_preset`, `resolve_effective_writing_preset`, `effective_personal_dictation_mode`, the `load_writing_profile` preset bit). Derive `ai_enabled` for the recording path; KEEP the stored key + `ai-enabled-changed` emit for frontend. Centralize the 8 invalidation sites behind one settings-mutation helper. Add the Rust↔TS golden-fixture parity test (first shared fixture: `migrate_preset_str↔migratePreset`, `requires_ai↔presetRequiresAiFormatting`, `default_for_ai_enabled↔defaultPresetForAiEnabled`, incl. `"Default"` under both ai_enabled flags — read by both `ai/tests.rs` and `ai.test.ts`).

## 12. Phase 4 — implementation blueprint (research-validated 2026-07-04, 3 parallel passes: codebase seams + agent-CLI headless + OpenRouter API)
Phase 4 = **three independent workstreams**, each ships alone: (A) OpenRouter provider [deferred here from Phase 3b — it needs backend catalog+runtime, not just UI], (B) app-context awareness, (C) agent-CLI providers (the no-API-key "local agent" category, §10). Note the OpenRouter mentions in §5-Phase3/§9.4 are stale on timing — OpenRouter lands in 4A, not 3.

**Ground-truth seam map (from live codebase, worktree HEAD):** providers are **data-driven from a JSON catalog, NOT a Rust enum**. Runtime dispatch is a 2-way switch in `AiExecutor::execute_once` (`ai/executor.rs:131-144`): `is_native_provider` (genai HTTP: OpenAI/Anthropic/Gemini, adapter-gated) vs `PROVIDER_CUSTOM` (OpenAI-compatible HTTP). Polish path: `writing::process_transcription` (`writing/pipeline.rs:382`) → `run_smart_formatting` (`pipeline.rs:292`) → `commands::ai::polish_text_typed` (`ai.rs:1037`, builds prompt via `ai/prompts.rs` + resolves provider/model from settings `ai_provider`/`ai_model`) → `executor.polish`. Keys: AES-256-GCM in `secure.dat` (encrypted tauri-store, **not** OS keychain), in-mem `API_KEY_CACHE`, names auto-derived `ai_api_key_<id>` from catalog `requires_api_key` (`ai.rs:32-38`). Catalog: `catalog/catalog.generated.json` ← `generate.py` ← models.dev snapshot + `overlay.json`; models already carry `recommended/reasoning/context/cost_input/cost_output` → picker renders new providers with **zero frontend catalog code**.
**Generic new-provider checklist (tight):** (1) `catalog/overlay.json` (+run `generate.py`) → provider+models; (2) `ai/genai_runtime.rs:92-109` adapter mapping [HTTP] OR new runtime module + `ai/mod.rs` [spawn]; (3) `ai/executor.rs:131-144` dispatch branch [only for non-genai]; (4) `commands/ai.rs:919-1007` `executor_for_provider` key/base-URL/no-auth wiring; (5) key storage automatic; (6) `EnhancementsSection.tsx:84-88` `GUIDED_PROVIDER_IDS/LABELS` + `src/types/providers.ts:41-58` `PROVIDER_UI_METADATA` (color+apiKeyUrl); (7) NO new command registration.

**4A — OpenRouter (first-class, curated).** OpenAI-compatible: base `https://openrouter.ai/api/v1`, `Authorization: Bearer`, add `HTTP-Referer: https://voicetypr.com` + `X-Title: VoiceTypr` app-attribution headers. Maps onto **genai `AdapterKind::OpenAI` + base-URL override** (`genai_runtime.rs:19-43` `with_endpoint_overrides`, wired at `executor.rs:32-52` + `ai.rs:919-1007`) — no new runtime. `overlay.json` entry (`requires_api_key: true`, `supports_base_url`, adapter OpenAI). **Curated ~6-model shortlist in the picker** (founder's "aggregators-with-hundreds-of-models can fuck us up" concern), custom-endpoint stays the escape hatch for arbitrary models. **Recommended default pair: primary `google/gemini-2.5-flash-lite` ($0.10/$0.40 per M), fallback `openai/gpt-4.1-nano` ($0.10/$0.40)** — both non-reasoning + fast + different upstreams; wire fallback via top-level `"models":[...]`. **AVOID reasoning models** (gpt-5-nano/mini, o4-mini, gemini-3.x-flash) — hidden reasoning tokens add 1–3s latency, fatal for polish. Gotchas: `:free` variants (20 rpm / 50-1000 rpd) unsuitable as default; `402`=no credits, `429`=respect Retry-After; catalog drifts (validate saved model id on startup, fall back to default pair). Optional: `provider:{sort:"throughput"}` or `:nitro` suffix for lowest latency.

**4B — app-context awareness (seam already exists).** `capture_active_app_context` (`writing/pipeline.rs:231`) already reads `active_win_pos_rs::get_active_window()` and stores `ContextHint{app_name}` (`writing/settings.rs:185`) — but only when `app_rules_need_active_app` gates it true (`pipeline.rs:388`). Work: (1) drop the gate so capture always runs when polish is on; (2) extend `ContextHint` with `bundle_id`/`window_title` (the `ActiveWindow` already exposes title+process path on both macOS+Windows via the one code path — only `.app_name` is read today); (3) maintained **app→category taxonomy** (chat/email/docs/code) with baked-in behaviors (strip trailing period in chat, structured prose in docs, camelCase in editors); (4) inject a light context line into the prompt at `build_enhancement_prompt_for_transcript_language` (`prompts.rs:220`, which already appends the context-term list at `:257`). **Local-only + transparent** (history shows what category was applied). Per-app overrides = the existing Advanced `AppFormattingRule` layer on top.

**4C — agent-CLI providers (new spawn runtime; see §10 for the detailed CLI research, re-verified live 2026-07-04).** New module `ai/agent_cli.rs` implementing `async fn polish(&AiPolishRequest)` (mirror `openai_compatible.rs`), declared in `ai/mod.rs`; add as an `AiExecutor` field + a dispatch branch in `execute_once` keyed on new provider-id constants (alongside `PROVIDER_CUSTOM` in `ai/providers.rs`); `executor_for_provider` gets a **no-API-key** branch (CLIs auth out-of-band). Catalog entry with no `adapter` (so it's not genai-routed) + empty model list (like `custom`). **Re-verified flags (all five installed): pi/omp cleanest** — `pi --no-tools --mode json --system-prompt "<tiny>" -p "<text>"` / `omp` identical (oh-my-pi fork, binary `omp`, auth in `~/.omp` — separate `/login`); **Claude Code** — `claude --bare -p --tools "" --system-prompt "<tiny>" --output-format json` (Keychain OAuth, no key); **Codex** weakest (no `--system-prompt` flag — config `model_instructions_file` only; shell tool not cleanly removable); **opencode** needs a pre-defined agent config file (`--agent polish`). **Detection = capability probe** (`--version` + auth-status subcommand), **NEVER read credential files** (trips credential-materialization guards). Mandatory hardening per §10 (kill-timeout → raw-transcript fallback, empty temp cwd, pinned-JSON validation, setup-time auth preflight).

**PENDING FOUNDER FORKS (asked 2026-07-04, defaults below if unanswered — do NOT start Codex on 4C until resolved):**
- **CLI set for first cut** — recommend **Claude Code + pi + omp** (all three have first-class replace-prompt+no-tools+JSON flags + subscription OAuth); Codex (config-file prompt, unstrippable shell) + opencode (needs agent config) as fast-follow.
- **Spawn model** — recommend **cold-spawn first (with 'Preparing…' pill + kill-timeout), warm rpc/serve session as a measured fast-follow** rather than warm-persistent lifecycle in v1. (§10 earlier leaned warm-by-default; the concrete lifecycle cost — health/restart/shutdown — argues for shipping cold-correct first.)

**4-sub-slot order (each: Codex implements → Claude gates+reviews → founder ping):** 4A OpenRouter (self-contained, lowest risk, unblocks "recommended pair" copy) → 4B app-context (small, seam exists) → 4C-i CLI runtime + one provider end-to-end (Claude Code or pi) → 4C-ii remaining CLIs + warm session. Copy owed to founder before ship: OpenRouter recommended-pair blurb + the "Which AI should I pick?" explainer (founder wants **setup guidance**, not just which-to-pick) + the CLI "no API key needed — uses a coding CLI you already have, slightly slower, shares your subscription limits" label.
